import { AoiDimensionsPipe } from './aoi-dimensions.pipe';

describe('AoiDimensionsPipe', () => {
  it('create an instance', () => {
    const pipe = new AoiDimensionsPipe();
    expect(pipe).toBeTruthy();
  });
});
