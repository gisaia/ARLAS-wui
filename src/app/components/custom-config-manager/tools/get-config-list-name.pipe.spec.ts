import { GetConfigListNamePipe } from './get-config-list-name.pipe';

describe('GetConfigListNamePipe', () => {
  it('create an instance', () => {
    const pipe = new GetConfigListNamePipe();
    expect(pipe).toBeTruthy();
  });
});
