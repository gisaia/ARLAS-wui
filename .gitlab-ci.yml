before_script:
  - ./scripts/prebuild.sh
lint:
  script:
  - npm run lint

test:
  script:
  - npm run test

build:
  script:
  - npm run build
  artifacts:
    paths:
    - dist
    name: "arlas-wui-dist"

